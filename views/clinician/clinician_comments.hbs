<div class="topnav">
  <a href="/clinician/logout">Back</a>
  <div class="title"><h1><b>Recent Comments From Your Patients</b></h1></div>
</div>

<table>
  <tr>
    <th class="intro">Patient</th>
    <th class="intro">Comment</th>
    <th class="intro">Date</th>
  </tr>

  {{#each rows}}
    <tr>
      <td>{{name}}</td>
      <td>
        <a href="{{patientURL}}">
          {{comment}}
        </a>
      </td>
      <td>{{date}}</td>
    </tr>
  {{/each}}
</table>